include _mixins
button.toggle-add-card(
  id="btn-add-card-" + list.id
  type="button", 
  _="on click toggle .hidden on me toggle .hidden on #add-card-" + list.id
  )
  +icon-plus()
  span  Add another card
